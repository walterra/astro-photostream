/**
 * Minimal Configuration Example
 * 
 * This shows the absolute minimum setup needed.
 * Everything else uses sensible defaults.
 */

export default {
  // Most basic setup - everything else uses defaults
  gallery: {
    itemsPerPage: 15
  },
  
  seo: {
    siteName: 'My Photo Gallery'
  }
};